# Django Framework & Related
Django==4.2.13
djangorestframework==3.15.2

# API Documentation
drf-yasg==1.21.7

# Authentication & Authorization
djangorestframework-simplejwt==5.5.0
dj-rest-auth==5.1.0
django-allauth==64.2.1

# Utilities
drf-nested-routers==0.95.0
python-dotenv==1.0.1
requests==2.32.3
geopy==2.4.1

# Production Deployment (WSGI/ASGI Server)
gunicorn==22.0.0
daphne==4.1.2

# Database Driver (entrypoint.sh에서 동적으로 설치)
psycopg2-binary==2.9.11

# --- S3/MinIO 스토리지 ---
django-storages==1.14.4
boto3==1.34.144

# --- 이미지 처리 ---
Pillow==10.4.0

# --- 지도 ---
contextily==1.6.2

# --- 비동기 작업 큐 & WebRTC 시그널링 ---
celery==5.4.0
redis==5.0.8
channels==4.0.0
channels-redis==4.2.0

# --- MinIO 클라이언트 ---
minio==7.2.7

# --- 지리공간 데이터 처리  ---
geopandas==1.0.1
matplotlib==3.9.1
polyline
elevation==1.1.3

# Test Code
Faker>=18.0.0
